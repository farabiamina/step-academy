let num: number = 5;

function myFuction(num: number, message: string) : void {
  console.log();
}

type my_cards = "visa" | "mastercard";

type currency = "EUR" | "USD";

type my_card_currency = `${my_cards}_${currency}`;

let aaa: string;




export enum Model {
  no = 0,
  yes = "yes",
}

// let phone1_model = Model.Apple;

let models = Object.values;

let models2 = Object.keys(Model);





